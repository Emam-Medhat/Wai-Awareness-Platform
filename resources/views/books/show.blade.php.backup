@extends('layouts.app')

@section('title', $book->title)

@section('content')
<div class="min-h-screen bg-gray-50">
    <!-- Book Header -->
    <div class="bg-gradient-to-r from-blue-600 to-purple-600 text-white">
        <div class="container mx-auto px-4 py-12">
            <div class="flex flex-col md:flex-row items-center gap-8">
                <div class="w-full md:w-1/3">
                    <div class="bg-white/10 backdrop-blur-sm rounded-lg p-8 text-center">
                        <i class="fas fa-book text-8xl mb-4"></i>
                        @if($book->is_featured)
                            <span class="inline-block bg-yellow-400 text-yellow-900 px-3 py-1 rounded-full text-sm font-semibold mt-4">
                                كتاب مميز
                            </span>
                        @endif
                    </div>
                </div>
                
                <div class="w-full md:w-2/3">
                    <h1 class="text-4xl font-bold mb-4">{{ $book->title }}</h1>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
                        <div>
                            <p class="text-lg opacity-90"><i class="fas fa-user ml-2"></i> المؤلف: {{ $book->author_name }}</p>
                            <p class="text-lg opacity-90"><i class="fas fa-building ml-2"></i> الناشر: {{ $book->publisher }}</p>
                            <p class="text-lg opacity-90"><i class="fas fa-tag ml-2"></i> التصنيف: {{ $book->category }}</p>
                        </div>
                        <div>
                            <p class="text-lg opacity-90"><i class="fas fa-file-alt ml-2"></i> عدد الصفحات: {{ $book->pages }}</p>
                            <p class="text-lg opacity-90"><i class="fas fa-language ml-2"></i> اللغة: {{ $book->language == 'ar' ? 'العربية' : 'الإنجليزية' }}</p>
                            <p class="text-lg opacity-90"><i class="fas fa-eye ml-2"></i> المشاهدات: {{ number_format($book->views) }}</p>
                        </div>
                    </div>
                    
                    <div class="flex gap-4">
                        <a href="{{ route('books.download', $book->slug) }}" 
                           class="bg-green-600 text-white px-6 py-3 rounded-lg hover:bg-green-700 transition inline-flex items-center">
                            <i class="fas fa-download ml-2"></i>
                            تحميل الكتاب
                        </a>
                        <button onclick="window.history.back()" 
                                class="bg-gray-600 text-white px-6 py-3 rounded-lg hover:bg-gray-700 transition inline-flex items-center">
                            <i class="fas fa-arrow-right ml-2"></i>
                            عودة
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Book Content -->
    <div class="container mx-auto px-4 py-12">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <!-- Main Content -->
            <div class="lg:col-span-2">
                <div class="bg-white rounded-lg shadow-md p-8">
                    <h2 class="text-2xl font-bold mb-6">وصف الكتاب</h2>
                    <div class="prose prose-lg max-w-none">
                        <p class="text-gray-700 leading-relaxed">{{ $book->description }}</p>
                    </div>
                    
                    @if($book->published_at)
                        <div class="mt-8 pt-8 border-t">
                            <p class="text-gray-600">
                                <i class="fas fa-calendar ml-2"></i>
                                تاريخ النشر: {{ $book->published_at->format('Y/m/d') }}
                            </p>
                        </div>
                    @endif
                </div>
            </div>
            
            <!-- Sidebar -->
            <div class="lg:col-span-1">
                <!-- Statistics -->
                <div class="bg-white rounded-lg shadow-md p-6 mb-6">
                    <h3 class="text-xl font-bold mb-4">الإحصائيات</h3>
                    <div class="space-y-3">
                        <div class="flex justify-between">
                            <span class="text-gray-600">عدد المشاهدات</span>
                            <span class="font-semibold">{{ number_format($book->views) }}</span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">عدد التحميلات</span>
                            <span class="font-semibold">{{ number_format($book->download_count) }}</span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">حالة النشر</span>
                            <span class="font-semibold text-green-600">منشور</span>
                        </div>
                    </div>
                </div>
                
                <!-- Related Books -->
                @if(isset($relatedBooks) && $relatedBooks->count() > 0)
                    <div class="bg-white rounded-lg shadow-md p-6">
                        <h3 class="text-xl font-bold mb-4">كتب ذات صلة</h3>
                        <div class="space-y-4">
                            @foreach($relatedBooks as $relatedBook)
                                <div class="flex items-center gap-4">
                                    <div class="w-16 h-16 bg-gradient-to-br from-blue-100 to-purple-100 rounded flex items-center justify-center">
                                        <i class="fas fa-book text-blue-600"></i>
                                    </div>
                                    <div class="flex-1">
                                        <h4 class="font-semibold line-clamp-1">{{ $relatedBook->title }}</h4>
                                        <p class="text-sm text-gray-600">{{ $relatedBook->author_name }}</p>
                                    </div>
                                    <a href="{{ route('books.show', $relatedBook->slug) }}" 
                                       class="text-blue-600 hover:text-blue-700">
                                        <i class="fas fa-arrow-left"></i>
                                    </a>
                                </div>
                            @endforeach
                        </div>
                    </div>
                @endif
            </div>
        </div>
    </div>
</div>
@endsection
